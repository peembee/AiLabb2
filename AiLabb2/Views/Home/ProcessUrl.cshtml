@model Dictionary<string, string>
@{
    ViewData["Title"] = "ProcessUrl";
}

<h1>ProcessUrl</h1>
<ul>
    @if (Model != null)
    {
        @foreach (var description in Model)
        {
            <li>@description.Key</li>
            <p>@description.Value</p>
        }
    }
</ul>
